/// เทมเพลตของ map_secrets.dart — ไฟล์นี้ถูก commit ขึ้น git ได้ปกติ (ไม่มีโทเคนจริง)
///
/// วิธีใช้:
/// 1) คัดลอกไฟล์นี้ แล้วเปลี่ยนชื่อเป็น "map_secrets.dart" (อยู่โฟลเดอร์เดียวกัน)
/// 2) เปิด map_secrets.dart แล้ววาง Mapbox Access Token ของคุณแทน placeholder ด้านล่าง
/// 3) map_secrets.dart อยู่ใน .gitignore แล้ว จะไม่ถูก push ขึ้น git โดยไม่ตั้งใจ
///
/// สมัคร token ฟรีได้ที่ https://account.mapbox.com/
const String mapboxAccessTokenSecret = 'ใส่_MAPBOX_ACCESS_TOKEN_ของคุณตรงนี้';
